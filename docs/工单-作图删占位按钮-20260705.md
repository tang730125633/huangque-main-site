# 工单 · 作图页删除无功能的占位（灵感描述按钮 + 灵感案例区）

> 供其他 agent 独立执行。先读《工单-其他agent实施》§0 +《任务看板》+ `DESIGN.md`。
> **冲突组：E-banana（`site/workbench/banana.html`）** —— 抢 `lock/E-banana`。**纯前端。**
> 来自 kongli：作图页有几个"有按钮/无功能"的占位，点了没反应（Issue #152、#146），直接删掉，别误导用户。

## 背景
作图页 `banana.html` 有占位 UI 没接线，点击无反应：
- **#152**：「提示词」标题旁的「灵感描述」按钮（约 29 行）——无 id、无 onclick、无 JS 绑定。
- **#146**：「灵感案例·做同款」区（约 82-97 行）——标题 + "换一批"按钮 + 5 个分类 chip + 5 张案例图，**都没绑点击**，纯装饰。

kongli 决定：**这两处都删掉**（不实现，作图页保持精简）。

## 改动（只改 `site/workbench/banana.html`）
1. **删「灵感描述」按钮**（约 29 行的那个 `<button>…灵感描述</button>`）。删后"提示词"标题行只剩标题，排版正常。
2. **删「灵感案例·做同款」整块**（约 81-98 行）：标题行(灵感案例·做同款 + 换一批按钮) + 分类 chip 行(美业海报/节日热点/国风雅韵/赛博光感/漫画插画) + 5 张案例图(meiye_poster/duanwu/huangque_poster/cyberpunk/comic)。
3. 若有 JS 引用到这些被删元素的（应该没有，本就没绑），一并清理，确保**无悬空引用、无控制台报错**。

## 边界
- 只删这两处占位，不动作图主流程（提示词输入、引擎、清晰度、比例、数量、参考图/生成/结果/历史）。
  > 注：若 IMG-EDIT（删参考图+数量改增减器）已先合并，注意二者都改 banana.html——**同组串行**，后领的基于最新 main 做，避免冲突。
- 保持 `DESIGN.md` 视觉，删除后周边留白/间距协调。

## 验收标准
1. 作图页**不再有「灵感描述」按钮、不再有「灵感案例·做同款」区**。
2. 删除后排版正常、无空洞、无控制台报错。
3. 作图主流程（出图/引擎/清晰度/数量/历史等）不受影响。

## 部署与验证
- `./ship "作图删灵感描述按钮+灵感案例占位(#152/#146)" site/workbench/banana.html`
- 实测作图页无那两处占位、出图正常。完成后可关 Issue #152 + #146。

> 本工单只做规划，未改动任何文件。
