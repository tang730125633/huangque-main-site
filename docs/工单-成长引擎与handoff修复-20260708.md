# 工单 · 轻量成长引擎（邀请/签到）+ 修跨页 handoff 断点（Issue #202，epic）

> 冲突组：**A（后端邀请/签到/成长值）+ shell（全站）+ 多个 E（各页 handoff）**。**epic，分子项领**。先读 §0+看板。

## 目标
无成长/会员/邀请/签到，留存与自增长为零；跨页 handoff 脆弱。分两块：

## 子项 A · 成长引擎（成本低见效快）
1. **邀请得点**（A+E-settings/recharge）：邀请码/链接，被邀请注册→双方得点。
2. **每日签到**（A+shell/E）：签到得点/成长值，连续签到加成。
3. **消耗成长值**：成长值兑换/等级权益（设计后定）。

## 子项 B · 修 handoff 断点（体验债，多为小修）
- `collect→banana` 单向：反向/双向打通。
- `script→作图/口播` 不带参：跳转带提示词/参数。
- 资产「重生成」静默重跑：⚠️ **部分已修**——VID-REGEN-MODE #333 已让视频资产重生成按原模式回填；核对图片/其它资产是否还有静默重跑。
- `video 任务不进 HQTasks`：视频任务接全局 tracker（与 #199 子项②同一件事，别重复做）。

## 验收
- 有邀请/签到闭环；跨页跳转带上下文、任务全局可续。

## 备注
- epic。handoff 断点多是小修可快速见效；邀请/签到是新功能需设计点数规则（找 kongli 定）。
- ⚠️ 与 #199②（全局 tracker）、#333（资产重生成）有重叠，领前核对别重复。分子项工单/PR。完成关 #202。
