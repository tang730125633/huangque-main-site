# 工单 · 灵感库「风景」分类扩充（Issue #309）

> 供其他 agent 独立执行。先读《工单-其他agent实施》§0 +《任务看板》+ `DESIGN.md`。
> **冲突组：E-inspiration（`site/workbench/inspiration.html`）+ F（`site/assets/inspirations/` 素材图）** —— 抢 `lock/E-inspiration`。**前端+素材为主**，一般不动后端。
> 相关：灵感页历次（铺满115张、9分类3D图标、灵感搜索、INSP-MODAL 详情弹窗一键跟创）。

## 现象（#309）
灵感库「风景」分类**只有 1 张**案例，远少于其它分类，体验不完整。yuelei 已备好 10 条风景案例数据（含标题/子类型/风格/提示词）。

## 现状（已排查 2026-07-07）
- 灵感数据在 `site/workbench/inspiration.html`（"风景"出现 2 处，即分类 chip + 那 1 张卡片）。
- 素材图在 `site/assets/inspirations/`。

## 待做
### 1. 生成 10 张风景封面图
按 yuelei 提供的 10 个子类型/提示词，用**作图引擎（GPT-image 或 NB2）生成 10 张风景图**：
| # | 标题 | 子类型 | 风格 |
|---|---|---|---|
|1|日落山海|日落|电影级光影 16:9|
|2|晨光森林|森林|丁达尔光效 治愈|
|3|星空湖畔|星空|银河倒映 冷色调|
|4|碧海蓝天|海景|热带度假 明亮|
|5|雪山之巅|雪山|日照金山 冷峻|
|6|樱花季|樱花|日系清新 唯美|
|7|城市夜景|城市|蓝调时刻 赛博写实|
|8|沙漠驼影|沙漠|暖金曲线 极简|
|9|秋色湖光|湖泊|阿尔卑斯秋色 镜面倒映|
|10|秘境瀑布|瀑布|热带雨林|

- 图片规格对齐现有灵感卡片（尺寸/比例/webp 优化，参考 ASSET-THUMB #225 的 COS webp 缩略思路，避免大图裂图）。
- 命名规范对齐现有 `site/assets/inspirations/` 里的文件（**先看现有命名再定**，保持一致）。
- ⚠️ 生成走作图引擎会**消耗点数/API**；建议用测试账号或运维额度，别扣真实用户点数。

### 2. 加进灵感数据 + 分类
- 在 `inspiration.html` 的灵感数据结构里，给「风景」分类**追加这 10 条**（标题+提示词+图片路径+子类型/风格标签，结构对齐现有条目）。
- 确认「风景」分类 chip 已存在（现在有 1 张说明 chip 在）；追加后该分类应显示 11 张（原1+新10）。
- **XSS**：标题/提示词进 DOM 用 `escapeHtml`（灵感页已有转义器，复用）。

## 验收标准
1. 灵感库「风景」分类筛选后展示 ≥10 张卡片（原1+新10），封面图正常加载不裂图。
2. 每张卡片标题/风格正确，点击详情/一键跟创（INSP-MODAL）能带出对应提示词。
3. 图片走 COS/webp 优化，加载不慢、不裂。
4. 不影响其它分类和灵感页功能（搜索/分类切换/跟创）。

## 部署与验证
- 素材图上传 COS/`site/assets/inspirations/` + `inspiration.html` → ship 到 webroot。
- ⚠️ **部署后公网 vs git 全文对比 + 实测风景分类展示 10+ 张、图不裂**。完成后关 #309。

## 备注
- 这是**内容+前端**任务，重点在**生成 10 张合格风景图**（质量按 DESIGN 金调美学，别用低质图凑数）。
- 若无作图额度/账号，需 kongli 提供，属阻塞点——先确认额度再开工。

> 本工单只做规划。核心：生成 10 张风景图 + 追加灵感数据，让风景分类不再只有 1 张。
