# 工单 · 视频页收尾成「美业出片工厂」（Issue #199，epic）

> 冲突组：**B（video.html）+ A（content_domains）+ 可能 shell（全局 tracker）**。**epic，按子项分批领**，每批只锁它实际改的组。先读 §0+看板。

## 目标（美业最高客单）
视频页三链路（口播/动作模仿/换装换背景）能跑，但右栏死 UI、参数写死、无 BGM/批量、离页丢进度。分子项做：

## 子项（可分批领，标注进度）
1. **放开参数 + 清死 UI**（B+A）：
   - ⚠️ **比例参数已部分完成**：motion 比例 #349、果肉 grok 比例 #375、数字人比例本就有。
   - 剩：分辨率/时长参数放开 + 清右栏写死/误导的死 UI（四态收口，别留假控件——参考 FAKE-UI 系列）。
2. **任务接全局 tracker**（shell/A）：视频任务接入全局 HQTasks，离页/刷新回来进度不丢（现在离页丢进度）。
3. **BGM**（A+B）：出片可选背景音乐（音轨合成，ffmpeg 混音已有 ffmpeg 4.4.2）。
4. **批量出片**（A+B）：一文案 × 多形象批量生成（配合 JOB-QUEUE-SPLIT 快慢池 #357 + 单用户限流 #304）。

## 验收
- 参数可调且真生效（非假控件，上线查 jobs 表确认）；离页回来进度不丢；支持批量。

## 备注
- 这是 epic，**别一个 PR 全做**。建议顺序：先②全局 tracker（体验痛点）→ ①清死UI放参数 → ④批量 → ③BGM。
- 每子项独立工单/PR，锁各自的组。完成全部关 #199。
