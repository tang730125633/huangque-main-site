# IP12 T1–T4 阶段验收报告

结论：`HOLD`
日期：2026-08-24
分支：`codex/ip12-runtime-v1-a-baseline-20260824`

## 分阶段结果

| 门禁 | 结果 | 证据 |
|---|---|---|
| T1 验收合同与真实对话语料 | PASS | 冻结合同；44 条永久语料；9 类状态预设；统一 Schema/安全/路由/幻觉评分 |
| T2 Provider 参数兼容套件 | OFFLINE PASS / LIVE BLOCKED | 10 类 canonical 请求、11 次 transport 调用、`pass/fail/unknown/blocked` 判定；无真实凭证/费用授权 |
| T3 custom vs agents_sdk 同语料 Eval | CONTRACT PASS / LIVE BLOCKED | 同一 `AgentDecision`、状态化 memory、延迟/错误计数、非法结构/引用/超时/流中断回退测试；没有真实模型质量、usage 或成本证据 |
| T4 泽龙 SDK 零扣点 Canary | NOT STARTED / HOLD | T2/T3 的真实 Provider 前置未通过，线上必须继续 `custom`、SDK 关闭 |

## 已完成

- 产品黄金路径、架构真相、红线、证据分层和 GO/NO-GO 已固化。
- 语料新增事实问答、事实纠正、运行中聊天、声音训练中聊天、缺形象/音色、刷新指代、多 production、重复确认、两类注入和隐私字段索取。
- 统一评分不允许平均值掩盖任一付费、安全、隐私或幂等失败。
- Provider 套件能够证明 Structured Output、强制工具、SSE、续轮、reasoning、store、usage、模型身份、错误与超时；HTTP 200 但缺行为证据时不会 PASS。
- 未识别 SDK Provider 现在 fail closed；DashScope 未通过 conformance 时在导入 SDK 前 fail closed。
- Cognitive Engine health metrics 只记录调用/回退计数和异常类型，不记录异常正文、客户原话或工具参数。
- T4 feature flag 还必须通过 SHA-256 固定且未过期的 live conformance artifact；报告绑定 release、Provider、model、corpus，并要求 Provider request ID/请求指纹相关、Eval 全部硬门槛通过。
- Agents SDK Master 与 Specialist 均显式设置 `ModelSettings(store=False)`；关闭 Trace 不再被错误地当作禁止 Provider 存储。

## 当前阻塞

1. OpenAI 官方、DashScope 与泽龙中转没有经 Tang 授权的真实测试凭证、模型和最大文本调用费用。
2. 泽龙中转没有独立 Agents SDK adapter 或可审计的 Responses 协议合同。
3. DashScope 当前 Chat adapter 的严格 JSON Schema 行为未通过同一探针。
4. 部署 requirements 尚未安装锁定的 Agents SDK；因此泽龙不能安全打开 feature flag。
5. 当前语义回复不是 Provider 原生 SSE delta；现有 SSE 只传耐久业务事件。

## 验证

- IP12 聚焦回归：`217 passed / 3 skipped`。
- 跳过项包括真实模型语料与可选 Agents SDK；不能把跳过当 PASS。
- 真实 Provider 请求：0。
- 黄雀报价、确认、上传、生成、Job、扣点：0。
- 泽龙服务、真实 Project、主站：未修改、未重启、未部署。

## 唯一下一授权

如要解除 HOLD，请 Tang 一次性明确：

- 三个 Provider 中本轮允许测试的具体 Provider；
- 具体模型；
- 可使用的受保护凭证；
- 最大文本调用次数和费用；
- 是否允许 T1–T3 PASS 后在泽龙启用只读、零黄雀点数 SDK Canary。

未获得以上授权前，不运行真实 Provider Eval，不启用 T4，不进入 T5 或 T17。
