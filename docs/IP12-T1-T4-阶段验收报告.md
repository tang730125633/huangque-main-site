# IP12 T1–T4 阶段验收报告

结论：`HOLD`
日期：2026-08-24
分支：`codex/ip12-runtime-v1-a-baseline-20260824`

## 分阶段结果

| 门禁 | 结果 | 证据 |
|---|---|---|
| T1 验收合同与真实对话语料 | PASS | 冻结合同；44 条永久语料；9 类状态预设；统一 Schema/安全/路由/幻觉评分 |
| T2 Provider 参数兼容套件 | LIVE PASS | OpenAI 官方 `gpt-5.6-terra` 10/10 通过；`live_capture`、请求指纹与时间相关；usage 完整、模型身份一致 |
| T3 custom vs agents_sdk 同语料 Eval | CONTRACT PASS / NOT STARTED | 同一 `AgentDecision`、状态化 memory、延迟/错误计数、非法结构/引用/超时/流中断回退测试；尚未执行 Terra 真实同语料质量对比 |
| T4 泽龙 SDK 零扣点 Canary | NOT STARTED / HOLD | T3 尚未通过，且 Tang 仍未授权 Canary；线上继续 `custom`、SDK 关闭 |

## 已完成

- 产品黄金路径、架构真相、红线、证据分层和 GO/NO-GO 已固化。
- 语料新增事实问答、事实纠正、运行中聊天、声音训练中聊天、缺形象/音色、刷新指代、多 production、重复确认、两类注入和隐私字段索取。
- 统一评分不允许平均值掩盖任一付费、安全、隐私或幂等失败。
- Provider 套件能够证明 Structured Output、强制工具、SSE、续轮、reasoning、store、usage、模型身份、错误与超时；HTTP 200 但缺行为证据时不会 PASS。
- 未识别 SDK Provider 现在 fail closed；DashScope 未通过 conformance 时在导入 SDK 前 fail closed。
- Cognitive Engine health metrics 只记录调用/回退计数和异常类型，不记录异常正文、客户原话或工具参数。
- T4 feature flag 还必须通过 SHA-256 固定且未过期的 live conformance artifact；报告绑定 release、Provider、model、corpus，并要求 Provider request ID/请求指纹相关、Eval 全部硬门槛通过。
- Agents SDK Master 与 Specialist 均显式设置 `ModelSettings(store=False)`；关闭 Trace 不再被错误地当作禁止 Provider 存储。

## 当前阻塞

1. T3 的 Terra 真实同语料质量、延迟、usage 与成本对比尚未执行；T4 不能开启。
2. 部署 requirements 尚未安装锁定的 Agents SDK；泽龙继续使用 `custom` Cognitive Engine。
3. 当前 Agent Runtime 与 Terra 探针仍在预览分支，尚未收拢进最新 `main` 和正式 PR。
4. 当前语义回复不是 Provider 原生 SSE delta；现有 SSE 只传耐久业务事件。

## 验证

- Provider 聚焦回归：`10 passed`。
- Terra 最终单轮：13 次请求，205 input / 430 output Token，估算 0.041775 元；Structured Output、强制工具、SSE、无存储续轮、reasoning、`store=false`、usage、模型身份、错误合同与超时全部 PASS，`evidence_correlated=true`。
- T2 四轮诊断累计 49 次请求，估算 0.15978 元；前两项探针合同缺陷均 fail closed 后修复，没有把异常写成 PASS。
- 零扣点黄金路径 Project `91997a00c4c8`：模块 1–4 与 PDF 确认、模块 5 的 3×10 选题、模块 6 的 3 篇完整口播、个人声音/形象推荐和 90 点报价均完成。
- 报价确认按钮可见但未点击；账号余额 94359→94359，Content Job 0→0，production 为 `quoted`、`job_id=null`、资产 0。
- Zelong 仅热装 Provider 验收脚本，无 Hermes 重启；主站未部署。

## 下一门禁

T2 已由官方 Terra 解除 HOLD。下一步先把当前预览分支的最小 Agent Runtime 与 Provider 合同收拢进最新 `main`；如需进入 T3，另行冻结真实语料调用次数与费用上限。T3 未 PASS、Tang 未单独授权 Canary 前，不启用 T4。
