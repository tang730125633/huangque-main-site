# 黄雀 · 任务看板

> **单一事实源：谁能领什么、谁在做什么、哪些会撞车。** 派活/领活前先看这里。
> 维护：kongli（+ 审核 AI）随任务推进更新状态。最后更新：2026-07-04（自助领取+组锁）

---

## 🚦 核心规则：按「冲突组」领，同组同时只能 1 个在做

两个任务只要**改同一个文件**就会撞车（合并冲突/互相覆盖）。所以按文件把任务分成**冲突组**——**同一组里，同一时刻只允许一个任务处于「🔵 进行中」**。不同组的任务可以并行。

| 冲突组 | 涉及文件 | 规则 |
|---|---|---|
| **A · 后端核心** | `server/content_api.py`、`server/content_domains/**` | 组内串行（一次一个）|
| **B · 视频页** | `site/workbench/video.html` | 组内串行 |
| **C · 资产页** | `site/workbench/assets.html` | 组内串行 |
| **D · 认证** | `server/auth_server.py` | 组内串行 |
| **imggen · 作图服务** | `server/imggen_api.py` | 组内串行 |
| **E · 其它前端页** | 某个具体 `workbench/*.html` | 按页面，改同一页才冲突 |
| **F · 独立** | `ci.yml`、`docs/**`、字体、脚本等 | 基本不冲突，可随意并行 |
| **admin · 运营后台** | `server/admin_api.py`、`site/admin/**` | 新建后台服务/页，组内串行 |

> ⚠️ 有的任务**跨组**（比如"批量生成"同时改 content_api 和 video.html → 占 A+B 两组）。跨组任务要等它占的**所有组**都空闲才能领，做的时候也**锁住这几组**。

---

## 📋 任务列表

**状态图例**：🟢 可领 ｜ 🔵 进行中 ｜ 🟡 待审(PR开着) ｜ ✅ 已完成 ｜ ⏸️ 阻塞(等素材/决定)

| ID | 任务 | 冲突组 | 状态 | 领取人 | 工单 |
|---|---|---|---|---|---|
| COS-PRIV | COS真人素材私有化(#185 P0隐私)【P1:对象键uuid不可猜→已上线；P2:私有ACL+预签名+接口鉴权+前端→待做】 | A+前端(tryon) | 🟢 可领(P2) | — | `工单-COS客户素材私有化与接口鉴权-20260706.md` |
| SEC-XSS | 前端XSS加固:统一转义器+采集页+CSP+token(#194 P1) | 多前端页+F+D | 🟢 可领 | — | `工单-前端XSS加固-20260706.md` |
| ARCH-DEBT | 后端架构债(#195 P1)【阶段1:鉴权短TTL缓存→已上线；阶段2:有界队列+拆core→待做】 | A | 🟢 可领(阶段2) | — | `工单-后端架构债重构-20260706.md` |
| FAKE-UI | 假控件清理三态收口:后台/video(#190 P1)【canvas已由CANVAS-NODE做真】 | 多前端页+admin | 🟢 可领 | — | `工单-假控件清理三态收口-20260706.md` |
| RESIL | 韧性:号池冗余+密钥拆分+端口收敛(#196 P1,部分需决策) | F/运维+A | 🟢 可领 | — | `工单-韧性合规号池密钥端口-20260706.md` |
| LEADGEN-PAGE | 获客采集按采集量翻页,修不同采集量结果相同(#227) | leadgen | 🟡 待审 | kongli | — |

### 已完成（近期，供参考，别重复做）
F1 形象库 · F2 参考动作 · F3 进度ETA · F4 字幕 · F6 模板库(已删除) · F7 资产增强 · T1/T2/T4/T5/T6/T7 · P1-5 缓存戳 · T8 content_api 拆分 · FB1 配音点数4→10 · FB2 作图点数动态同步 · MARK 资产收藏标签 · F8 换装换背景 · FB3 作图接口加固 · FB5 clone-vip校验加固 · FB4 生成隐私+改密拦截 · FB7 改密安全闭环 · ADMIN-1运营后台(8098) · COS作图(imggen) · tikhub域名收紧 · 采集视频转存COS · COS下载白名单修复 · ASSET-DEL资产删除 · DL-IMGS下载全部图 · DL-TYPE下载按类型命名 · 泽龙固定1024 · 灵感页铺满115张 · ADMIN-4后台优化+功能开关 · ADMIN-5调用日志 · 灵感页原创美业图 · COS转存超时修复 · FB6视频校验+成本20 · ADMIN-2用户管理 · ADMIN-3充值审批 · FB8公共音色试听 · COS成片存储 · COS全产出直链(图/音/视) · tikhub抖音解析加固 · ADMIN-6调用日志功能列细化(读payload) · ADMIN-6b泽龙zelong2映射修复 · BANANA-FIX恢复局部修改/图生图+泽龙2引擎 · COPY-ASR复制文案升级音频转写(#169) · PROMPT-COUNT提示词字数计数/清空变活 · ASR-UP提取文案抽音轨+gpt-4o-mini-transcribe · INSP-MODAL灵感页详情弹窗一键跟创+48张 · DB-BACKUP每日DB异地备份COS(#188) · SVC-HARDEN六服务unit加固防崩溃循环(#186) · SEC-XFF限流改X-Real-IP堵撞密码(#189) · HOME-ICON首页卡片3D品牌图标试点 · COLLECT-TA采集口播文案常驻文本框 · REFUND-CAS退点幂等CAS堵重复退点(#187) · HEALTH-MON六服务健康监控+失败率告警(#193) · DB-GC jobs.db瘦身573→2MB+每日GC(#191) · INSP-SEARCH灵感搜索bug修复(#190) · PASTE-REF粘贴剪贴板图作参考图(作图+灵感跟创) · COS-PRIV-P1对象键uuid(#185) · ASR-SEM提取文案转写信号量止血 · PROG-UX进度流动光带+真实计时+采集进度条(#162) · ARCH-DEBT-P1鉴权短TTL缓存降auth负载(#195) · ASSET-THUMB资产页缩略图COS webp小图修裂图(#225) · REVERSE作图页提示词反推(图→Gemini→提示词→填框/图生图,补nginx精确路由+并发闸,#220) · CANVAS-NODE节点式生产画布(文本/图片/反推/作图·拖动+连线+调参+并行,#223)

---

## 🧭 自助领取流程（agent 自己领，靠「组锁」原子防冲突）

**认领的唯一凭据 = 组锁分支 `lock/<组>`**。创建 git 分支(ref)是**原子操作**：两个 agent 同时抢同一个组锁，只有一个能 push 成功，另一个报错——所以**抢到锁 = 合法认领，抢不到 = 该组已被占**。看板的 🔵 状态只是给人看的镜像，由维护者同步。

**步骤：**
1. 看板挑一个 **🟢 可领** 任务，看它的**冲突组**。
2. **抢组锁**（对它占的每个组 G，逐个抢）：
   ```bash
   git ls-remote origin refs/heads/lock/<G>          # 先看有没有被占(有输出=被占)
   git push origin origin/main:refs/heads/lock/<G>   # 抢锁：成功=抢到；报错(rejected/exists)=已被别人占
   ```
   - **跨组任务**(如 F5=A+B) 要把**每个组都抢到**；任一抢不到 → 把已抢到的用 `git push origin --delete lock/<组>` 退掉、换别的任务。
3. **抢到锁后立刻更新本看板该行**：状态改 **🔵 进行中** + 填**领取人**（一并提交，别等维护者——看板要实时反映你已认领）。
4. 开分支 `codex/<任务ID>`，严格按对应《工单》+《开发手册》/§0 开发（走 PR、只走 ship、禁止直改服务器、署名 kongli）。
5. 开 PR（**别开成 draft，做完就 Ready**），把看板该行改成 **🟡 待审**。
   - 🚫🚫 **开完 PR 就停下，绝对不要自己合并/merge！** 合并 + 部署 + 上线由**审核 AI** 统一把关。你自己合会**绕过审核、且不会被部署到线上**，只会造成 git 与线上分叉。开完 PR 就等审。
6. **审核 AI 合并上线后**：释放你占的每个组锁 `git push origin --delete lock/<G>`。

> **看板维护分工**：**领取的 agent** 负责改自己那行为 🔵进行中(认领时)/🟡待审(交PR时)；**审核 AI** 负责合并后标 ✅、移入已完成、校正对齐、清理卡死的锁。

> **一句话规则：只领 🟢 且能抢到全部组锁的任务；抢不到就换一个或等。** 组锁名 = 组字母：`lock/A`(后端核心) `lock/B`(视频页) `lock/C`(资产页) `lock/D`(认证) `lock/E-<页名>`(其它页) —— F 独立组不用锁。
>
> 查当前被占的组：`git ls-remote origin 'refs/heads/lock/*'`

---

## ➕ 加新任务 / 反馈转任务

- 新需求或 Issue #60 的反馈 → 由 kongli/审核 AI 出《工单》入 `docs/`，在本看板加一行（标好**冲突组**和状态）。
- 加行时**必须标冲突组**（按它要改的文件归组），否则并行会撞车。

---

> 配套：《开发手册-黄雀网站.md》(怎么改)、《工单-其他agent实施》§0(公共上下文)、Issue #60(反馈台)。
