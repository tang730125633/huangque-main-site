# 黄雀 · 任务看板

> **单一事实源：谁能领什么、谁在做什么、哪些会撞车。** 派活/领活前先看这里。
> 维护：kongli（+ 审核 AI）随任务推进更新状态。最后更新：2026-07-10 续⑥（合并部署 #551/#549/#552/#553/#554 五个 PR，详见下方续⑥行）。历史：2026-07-09（字幕收口:位置5档#478+长句折行#481;设置页改密安全化#480;线路二WaveSpeed骨架上线#457;上传槽粘贴图片#483;VID-REGEN-MOTION#473/NAV-BELL#466/GROW-CRM意图分级#456三项审毕上线;补录TRYON-LINE-LABEL#482/SHIP-PORTABLE#479/POINTS-AUDIT#477已上线;新增VID-MOTION-DURATION(#476影级模仿时长被固定)已挂🟢可领;#475审毕代码OK但dirty冲突已打回rebase;新增TUTORIAL-VIDEOS(#489教程区正式教学视频)待审）
> 2026-07-10 大收口（审核AI）：本轮上线 #498教程视频(走COS/CDN,git内0mp4)/#499影级模仿原时长/#502线路一融合+线路二抽帧/#515出境超时/#475可访问性首切片/#517部署链硬化/#520资产库采集预览/#522编导脚本/#523教程占位清理/#506+#365封面回填(tang执行)。5个工单收口:GROW-FRONTEND首切片✅、VID-MOTION-DURATION/TUTORIAL-VIDEOS/ASSET-COLLECT-FIX/EDITOR-SCRIPT-ACTIONS 完成移入已完成。IMG-TEMPLATE-RATIO-SYNC/COLLECT-TIMEOUT 已领取(lock/E-banana、lock/E-collect)转进行中。关闭issue #365/#366/#413/#476/#488/#492/#493/#500/#501。⚠️安全:#512 七凭据+我新增COS_SECRET/egress节点明文泄露待kongli轮换。
> 2026-07-10 续②：关闭已修issue #500/#501(→#502)/#413(→#503通知中心);资产库/编导bug批#486-#495核实根因归并为4张可领工单(ASSET-DOC-VIEW文案名单查看#486/#487/#494、ASSET-COLLECT-FIX采集403+滚动条#488/#492、EDITOR-SCRIPT-ACTIONS编导按钮#493、IMG-TEMPLATE-RATIO-SYNC作图比例联动#495)
> 2026-07-10 续③：关闭 #366(motion失败=HeyGen慢,已由HEYGEN-DIRECT直连+reaper480s缓解,无新反馈);#365有PR #506(回填脚本审毕通过,因看板撞#508待rebase);新挂工单 COLLECT-TIMEOUT(#458采集超时)、INSP-DIVERSIFY(#449灵感样式,偏内容策划)
> 2026-07-10 续④（#525大收口之后）：补录 IMG-TEMPLATE-RATIO-SYNC(#526作图模板↔比例双向同步)、COLLECT-TIMEOUT(#524采集failed终态收尾) 两项✅已上线。**开放PR仅剩 #521**(ship部署安全闸门,与已合并#517撞车dirty)——审核后确认**不是重复、不可关闭**:它独有 `smoke_import`(从systemd ExecStart/WorkingDirectory反查入口做冒烟,覆盖全部服务而非#517硬编码只测content) 与 `check_restart_effective`(断言①ActiveEnterTimestamp>ship起始时刻证明restart真发生 ②无EnvironmentFile比启动新,证明进程加载最新配置)。已请作者rebase到含#517的main只叠加这两项。⚠️实测印证:content.env于13:14:48写入EGRESS_PRIMARY_TIMEOUT=300时服务未重启("进文件没进进程"),经后续#526/#524部署重启(imggen13:27/content13:47)已生效——正是check_restart_effective要防的复发场景。
> 2026-07-10 续⑤（审核AI一轮，开放PR归零）：上线 #521 ship重启闸门 / #527 资产库文案名单查看(ASSET-DOC-VIEW收口) / #529 浅色深色主题+设置页重整 / #530 通知跳资产库定位高亮 / #531 Seedream生图引擎(火山方舟Ark国内直连) / #532+#536 教程区「从获客到成交」正片(视频传COS,教程区10个视频全走CDN,git内0 mp4) / #533 果肉+泽龙2出图死线300→600s / #534 Seedream Pro高清必失败 / #535 图生图扣点前校验。**审核方在4个PR里拦下了会真实伤人的问题**：#531 出图POST被通用_retry包住(5xx/超时重发=重新出图=重复计费)→改只重试429；#535 validate=True会把#505/#483修好的「剪贴板粘贴带换行/反推回填缺padding」的base64在扣点前判400(功能彻底不可用)→改先_clean_b64清洗再校验，且参考图压JPEG会让gpt edits拿到「JPEG字节贴PNG标签」→改回PNG，另修一处缩放后尺寸未回传导致inpaint蒙版错位的隐藏bug；#529 theme.css/theme-init.js用手写死值?v=1而stamp_assets.py只认cloud-shell.js→泛化为多资源注册表(改主题CSS戳会变)；#534 魔数校验从引擎内(扣点后)提到validate_image_payload(扣点前,所有引擎受益且不扣点)。**ARK_API_KEY已配置并实测生效**(GET /models 200/0.36s直连,两个model id都在)。新挂工单 TRYON-LINE2-VAR(#537)。
> 📌 **部署纪律（本轮建立）**：①改 content_domains 要重启 **content+imggen-api+admin 三个** importer；②重启前 import 冒烟；③取**服务器时钟** T0，重启后断言 ActiveEnterTimestamp≥T0（证明 restart 真发生）；④**部署前先查在飞任务**，等空窗再重启（#511 未修，靠纪律规避——本轮 #535/#534 各等了 6~14 分钟窗口，零误杀）；⑤md5 MATCH 不等于部署对了——`git fetch` 失败时本地 origin/main 是旧的，md5 只证明"服务器==本地旧版"，必须同时校验**内容特征**（#527 部署时就靠这个抓到一次假绿灯）。
> ⚠️ **安全待办 #512**：7+个凭据明文泄露须轮换(HeyGen×2/OpenAI/OpenRouter/SSH密码/leadgen×2/DOUBAO_TOKEN);审核AI自查追加 COS_SECRET_ID+KEY(媒体桶读写) 与 egress VLESS节点凭据(UUID/IP/pubkey/shortId)。**纪律**:查env/单元一律只看键名不看值,凭据只落服务器600文件,永不进git/echo。
> 2026-07-10 续（审核AI一轮）：EGRESS-CHAIN作图出境优先级链#491上线+搭VPS隧道启用;MOTION-FIX线路一融合/线路二抽帧#502;NOTIFY-CENTER全站通知中心#503;IMG-FAILRATE作图失败率base64容错+退避重试#505 —— 四项审毕上线。#489教程视频关闭去重(收敛到#498)。开放PR #475/#499/#498 均打回rebase中。
> 2026-07-10 续⑥（审核AI一轮，开放PR再归零）：合并部署 **#551**采集页静默失败改可见提示 / **#549** Nano Banana2+Pro合并为单张引擎卡(引擎行6→5) / **#552** gpt-image-2修假比例(9:16与3:4曾同映射成2:3假图)+按官网token价重定点数(标4/高15,旧12点对1:1高清倒挂) / **#553** 视频任务接全局tracker(GROW-VIDEO-FACTORY子项②,离页/刷新不丢+404收口+断网保留可重试+kind隔离防视频与获客撞号) / **#554** 教程视频黑屏兜底。**审核方在4个PR里各拦下一个会真出问题的点**：#551 原PR把「点历史采集卡片」的fetch误当轮询、套 `clearInterval(pollIv)+showState('emptyState')`——用户边采集边点卡片、网络一抖就会误杀在跑的采集轮询并清空当前结果→第二处降级为只 setNote;#549 rebase时分支基于#547合入前的旧main、会删掉#547的提示词比例识别→解冲突两边都保;#552 逐条核前后端计费一致(前端gpt**不带**provider→后端cost_of/gen_image都默认openai→4/15,其余引擎带provider走各自8/12不变,banana的nb2/pro走独立imggen BASE_COST不碰此路)+线上cost_of 6例实测全过+SIZES全16倍数比例精确;#554 去掉`stalled→markBroken`过激终态(慢网/CDN抖动对**好视频**也误报,真损坏走的是error)。**根因查清(教程视频)**：`getting-started.mp4` 在COS上被截断(Content-Length声称9.46MB、实际只传~2MB就IncompleteRead)=确证黑屏元凶,前端兜底已止血但**需kongli给正确源mp4重传**;另9个教程视频非faststart(moov在文件尾)、大文件起播慢会先黑屏但**没损坏**,有源文件可批量`ffmpeg -movflags +faststart`无损重编。**部署纪律照常**:#552改共享content_domains已重启content+imggen-api+admin三importer、部署前查零在飞任务、ActiveEnterTimestamp≥T0、drift逐文件==origin/main。

---

## 🚦 核心规则：按「冲突组」领，同组同时只能 1 个在做

两个任务只要**改同一个文件**就会撞车（合并冲突/互相覆盖）。所以按文件把任务分成**冲突组**——**同一组里，同一时刻只允许一个任务处于「🔵 进行中」**。不同组的任务可以并行。

| 冲突组 | 涉及文件 | 规则 |
|---|---|---|
| **A · 后端核心** | `server/content_api.py`、`server/content_domains/**` | 组内串行（一次一个）|
| **B · 视频页** | `site/workbench/video.html` | 组内串行 |
| **C · 资产页** | `site/workbench/assets.html` | 组内串行 |
| **D · 认证** | `server/auth_server.py` | 组内串行 |
| **imggen · 作图服务** | `server/imggen_api.py` | 组内串行 |
| **E · 其它前端页** | 某个具体 `workbench/*.html` | 按页面，改同一页才冲突 |
| **F · 独立** | `ci.yml`、`docs/**`、字体、脚本等 | 基本不冲突，可随意并行 |
| **admin · 运营后台** | `server/admin_api.py`、`site/admin/**` | 新建后台服务/页，组内串行 |
| **shell · 全站外壳** ⚠️ | `site/workbench/cloud-shell.js`（全站共享左侧栏/导航） | **排他大组**：改 cloud-shell.js 会触发 `stamp_assets.py` 把**全部 workbench/*.html 的 `?v=` 版本戳都 bump**→与**任何**前端页任务冲突。**占 shell 期间禁止一切前端页(B/C/E/admin)任务并行**；有前端页任务在做时 shell 也不能领。 |

> ⚠️ 有的任务**跨组**（比如"批量生成"同时改 content_api 和 video.html → 占 A+B 两组）。跨组任务要等它占的**所有组**都空闲才能领，做的时候也**锁住这几组**。
>
> 🔒 **锁按「你 PR 实际会改的每个文件」来占，不是按任务名义！** 这是防冲突的命根子。开工前先想清楚要动哪些文件、逐一归组、把**每个**组都锁上；中途要多改一个文件 → 先补锁那个组（补不到就停）。**漏锁去改别人的组 = 违规**：即使锁了 shell 也不能顺手改 video.html（那是 B 组）。审核方会核对「PR 改的文件集 ⊆ 你锁的组」，超出直接打回（#244 就栽在只锁 shell 却改了 B/C/E 四个前端页）。

---

## 📋 任务列表

**状态图例**：🟢 可领 ｜ 🔵 进行中 ｜ 🟡 待审(PR开着) ｜ ✅ 已完成 ｜ ⏸️ 阻塞(等素材/决定)

### ⏳ 待 kongli 决策 / 给资源（卡在这几项，给了就能推进）
- **RESIL 备用账号**：号池故障切换代码已就绪，缺**备用账号密钥**(泽龙/HeyGen等)，只能 kongli 线下搞到再填。
- **教程视频重传**(#498/#554 根因)：`getting-started.mp4` 在 COS 上被截断(声称 9.46MB、实只传 ~2MB 就 IncompleteRead)=确证黑屏元凶，前端兜底 #554 已止血，但根因**需 kongli 提供正确源 mp4** 才能重传(损坏副本无法恢复)；另 9 个教程视频非 faststart(moov 在尾)起播慢，有源文件可批量 `ffmpeg -movflags +faststart` 无损重编。
- **已决策(存档)**：果肉生图速度239s→"先用着"(死线已放宽300s)；转码P2 faststart(moov前置)已上线#371,完整重编码转码CDN已上线暂不需；视频CDN加速→已上线(kongli配置#339)；8501端口→已收敛；泽龙2号池429→等厂商自维护。

| ID | 任务 | 冲突组 | 状态 | 领取人 | 工单 |
|---|---|---|---|---|---|
| RESIL | 韧性(#196 P1)【密钥拆分#268/端口审计#279/泽龙2号池#286→已上线;端口暴露审计报告→docs/端口暴露审计报告-20260707.md】⏸️阻塞:①端口收敛P0已做:cupsd631已关+8501小探改绑172.17.0.1仅内网(kongli决策收敛,公网已断);剩云安全组层面待线下核对②备用账号(暂无可填,代码留着) | F/运维+A | ⏸️ 阻塞(仅剩②备用账号) | — | `工单-韧性合规号池密钥端口-20260706.md` |
| GROW-VIDEO-FACTORY | 视频出片工厂(#199 epic,美业最高客单):放开分辨率/时长参数+清死UI(比例已#349/#375部分完成)/任务接全局tracker离页不丢/加BGM/批量出片。分子项领,建议序:tracker→清死UI→批量→BGM | A+B+shell(epic总范围) | 🟢 可领(**子项①参数/死UI #557 + ②tracker #553 + ④批量出片 #558 已上线**;仅剩 BGM 可领) | — | `工单-视频出片工厂-20260708.md` |
| GROW-TEMPLATE | 美业模板体系+门店立项(#200 epic):模板化(海报/团购/竖屏/九宫格/数字人首帧,占位品牌/主色/卖点/价格)扩「一键跟创」为「模板即出片」+门店档案立项自动带上下文(联动#197飞书群)。种子:152反推词+跟创 | A+E-banana+E-inspiration+admin | 🟢 可领(epic分批) | — | `工单-美业模板体系立项-20260708.md` |
| GROW-CRM | 获客CRM化(#201 epic):**阶段1+2已上线(#384/#390)**:确定性lead_id(sha1 platform:user:content)+去重更准+意图关键词标签+跟进状态/备注控件(localStorage按lead_id存+CSV导出)。**阶段3意图AI分级已上线(#456)**。**剩**:一键推客户群(共用#197推送,待#197飞书打通) | A+E-leads | ✅ 主体完成(剩推客户群待#197) | kongli | `工单-获客CRM化-20260708.md` |
| GROW-ENGAGE | 成长引擎+handoff修复(#202 epic):邀请得点+每日签到+成长值(需kongli定点数规则)；修跨页断点(collect→banana单向/script不带参/资产静默重跑部分#333已修/video任务不进tracker与#199重叠)。⚠️领前核对别重复 | A+shell+多E | 🟢 可领(epic分批) | — | `工单-成长引擎与handoff修复-20260708.md` |
| GROW-FRONTEND | 前端组件化+四态+可访问性(#203 epic工程债):抽公共JS(轮询/上传/戳/下载)+公共UI组件+四态范式(leads/audio标杆)+div改button加aria键盘可达+统一hash缓存戳。⚠️排他大组,必与业务前端PR错峰,一次一小块 | shell+全前端 | ✅ 首切片#475已上线(共享外壳可访问性:burger/登录tab/退出/登录卡div→button+aria,保留#503通知中心);epic后续仍可领 | kong74007-ui | `工单-前端组件化四态可访问性-20260708.md` |
| ASSET-DOC-VIEW | 资产库文案/名单查看修复(#486/#487/#494,审核AI核实归并):①名单(leads)在资产库无查看入口——docCard仅当x.url存在才渲染「打开」(assets.html:1529),名单非文件无url→压根没按钮/详情弹窗;且字段缺失(leads入assets表meta未带昵称/IP属地/需求原文)。②文案(copy)有openPromptModal(body)(assets.html:1522)但依赖body,打不开=body未入库/为空。改:leads补详情弹窗(昵称/IP/需求原文/来源视频)+后端leads→assets字段映射补全;copy确保body完整入库可查 | C(assets.html)+A(后端leads/copy入库映射) | ✅ 已完成(#527已上线:leads/copy详情弹窗全走createElement+textContent天然免疫XSS,外链URL协议白名单+rel=noopener,后端_LEAD_META_FIELDS白名单投影15字段,_copy_body把scenes格式化;已部署重启三importer) | kong74007-ui | 详见 #486/#487/#494 |
| IMG-TEMPLATE-RATIO-SYNC | 作图模板↔比例控件不同步(#495):选模板不联动下方比例控件,切比例不高亮对应模板,两者独立。加双向同步(选模板自动切比例+切比例高亮对应模板) | E-banana(banana.html) | ✅ 已完成(#526已上线:syncTemplateForRatio+selectRatio(next,syncTemplate)双向联动,4测试,已部署漂移bless) | kong74007-ui | 详见 #495 |
| COLLECT-TIMEOUT | 内容爬取超时时间过短+reaper无collect宽限致频繁超时失败(#458):①前端collect.html poll()超时400s,但采集+COS转存大视频+口播提取常超400s→改600s;②后端core.py reaper对tryon2400/video1800/xiaole1200/image900有特殊宽限,collect只走默认360s(前端还在等后端已杀)→reaper加collect 720s宽限;③collect.html+video.html poll补status===failed判断(同#391);根因清晰方案明确,可直接实施 | E-collect(collect.html)+A(core.py)+B(video.html) | ✅ 已完成(超时600s/reaper collect 1200s宽限先前commit已做;#524补collect主任务+提取口播文案轮询识别failed终态立即结束显示退点提示;video轮询failed由#396完成) | Codex | 详见 #458 |
| INSP-DIVERSIFY | 灵感案例图片样式过于单一(#449):题材/构图/配色重复度高,缺场景化行业化差异,连续浏览审美疲劳。⚠️偏内容策划/视觉素材层(非纯代码):需产出不同场景/行业/主体/构图的多样化案例图(INSP-EXPAND#421已扩至272张,本项要提升"多样性"而非数量)+强化图文对应。可与GROW-TEMPLATE美业模板体系联动 | E-inspiration(数据+webp素材) | 🟡 部分:#544已合并部署(移除214张同模板占位+4张原创案例),但删除时**7个美业垂类案例一并消失**→#449已重开补7垂类真实案例图(偏内容策划) | kong74007-ui | 详见 #449 |
| TRYON-LINE2-VAR | 换装线路二上传框变量名错误+切线路不清数据(#537,审核AI已核实):video.html 单一变量 tryonVideoData 被两条线路共用——1505行线路一存换装视频,1502行线路二存人物照片。隐患:用户线路一传视频→切线路二(applyTryonLine不清数据)→提交时把视频当 person_image_data 发后端→扣点后失败退点、白等。改:线路二用独立 tryonPersonImageData;applyTryonLine() 切换时清空旧数据并重置上传框。⚠️tryon 走 gen_tryon 不经 validate_image_payload,享受不到#534的扣点前魔数校验 | B(video.html) | ✅ 已完成(#537线路二独立tryonPersonImageData+切线路清数据;#545拆tryonVideoData/tryonPersonImageData按线路路由预览;#550修回填回归:tryonLineFromAsset按MIME/扩展名判线路、回填先恢复线路再setTryonPrefill,时长文案改实测中位数,已部署) | codex/kong74007-ui | 详见 #537/#545/#550 |
### 已完成（近期，供参考，别重复做）
COLLECT-CATCH采集页静默失败改可见提示(#551):两处空catch改为提示;⚠️审核方修:原PR把「点历史采集卡片载入结果」的fetch误当轮询套clearInterval(pollIv)+showState('emptyState')→边采集边点卡片网络一抖会误杀在跑的采集轮询+清当前结果,第二处降级为只setNote,已部署 · IMG-BANANA-CARD Nano Banana2+Pro合并为单张引擎卡(#549):引擎行6→5,后端零改仍打/api/gen/banana带model=nb2|pro,costKey按bananaVariant重算(切型号14→26立即更新),selectEngineByKey映射老深链nb2/pro→卡+型号,17测试;⚠️审核方rebase解冲突保住#547比例识别(分支基于旧main会删),已部署 · IMG-GPT-RATIO-PRICE gpt-image-2修假比例+重定价(#552):SIZES里9:16与3:4曾同映射1024x1536(=2:3假图)→改真实1152x2048/2048x1152/1200x1600(全16倍数);按官网$30/M image output token实测重定点数标4/高15(旧12对1:1高清倒挂);cost_of从只按quality改按provider分档IMAGE_BASE_COST(前端gpt不带provider→后端默认openai→4/15,其余带provider走8/12不变,banana走独立imggen BASE_COST不碰);16测试+线上cost_of 6例实测全过,改共享content_domains已重启三importer,已部署 · VID-GLOBAL-TRACKER视频任务接全局tracker(GROW-VIDEO-FACTORY#199子项②,#553):cloud-shell在全部页加载tasks.js(复用cloud-shell戳做版本),视频页4类提交入口登记job_id+持续写进度终态,刷新/离页经?task=或最近活动视频任务恢复轮询,404收口不再无限轮询,断网保留pending可重试,任务身份纳入kind防视频与获客job_id撞号,6测试,17前端文件(14戳bump+cloud-shell/tasks.js/video.html)已部署 · TUTORIAL-BLACKSCREEN教程视频黑屏兜底(#498/#554):mp4加载失败/超时抽成renderUnavailable兜底(error+2200ms定时器,定时器用networkState判断慢加载不误杀),不再只剩黑屏;⚠️审核方去掉stalled→markBroken过激终态(慢网对好视频误报,真损坏走error);根因查清getting-started.mp4在COS被截断(声称9.46MB实传2MB)=黑屏元凶需重传,另9个非faststart起播慢没坏,已部署 · DOUJIE-RENAME微衣视频更名豆姐视频:kongli拍板正式上线+更名,video.html/video.py/points.py/image.py/core.py用户可见文案改「豆姐视频」,内部channel=micro等标识符不变(#360,kongli本人直接审核+ship,已实测线上生效) · F1 形象库 · F2 参考动作 · F3 进度ETA · F4 字幕 · F6 模板库(已删除) · F7 资产增强 · T1/T2/T4/T5/T6/T7 · P1-5 缓存戳 · T8 content_api 拆分 · FB1 配音点数4→10 · FB2 作图点数动态同步 · MARK 资产收藏标签 · F8 换装换背景 · FB3 作图接口加固 · FB5 clone-vip校验加固 · FB4 生成隐私+改密拦截 · FB7 改密安全闭环 · ADMIN-1运营后台(8098) · COS作图(imggen) · tikhub域名收紧 · 采集视频转存COS · COS下载白名单修复 · ASSET-DEL资产删除 · DL-IMGS下载全部图 · DL-TYPE下载按类型命名 · 泽龙固定1024 · 灵感页铺满115张 · ADMIN-4后台优化+功能开关 · ADMIN-5调用日志 · 灵感页原创美业图 · COS转存超时修复 · FB6视频校验+成本20 · ADMIN-2用户管理 · ADMIN-3充值审批 · FB8公共音色试听 · COS成片存储 · COS全产出直链(图/音/视) · tikhub抖音解析加固 · ADMIN-6调用日志功能列细化(读payload) · ADMIN-6b泽龙zelong2映射修复 · BANANA-FIX恢复局部修改/图生图+泽龙2引擎 · COPY-ASR复制文案升级音频转写(#169) · PROMPT-COUNT提示词字数计数/清空变活 · ASR-UP提取文案抽音轨+gpt-4o-mini-transcribe · INSP-MODAL灵感页详情弹窗一键跟创+48张 · DB-BACKUP每日DB异地备份COS(#188) · SVC-HARDEN六服务unit加固防崩溃循环(#186) · SEC-XFF限流改X-Real-IP堵撞密码(#189) · HOME-ICON首页卡片3D品牌图标试点 · COLLECT-TA采集口播文案常驻文本框 · REFUND-CAS退点幂等CAS堵重复退点(#187) · HEALTH-MON六服务健康监控+失败率告警(#193) · DB-GC jobs.db瘦身573→2MB+每日GC(#191) · INSP-SEARCH灵感搜索bug修复(#190) · PASTE-REF粘贴剪贴板图作参考图(作图+灵感跟创) · COS-PRIV-P1对象键uuid(#185) · ASR-SEM提取文案转写信号量止血 · PROG-UX进度流动光带+真实计时+采集进度条(#162) · ARCH-DEBT-P1鉴权短TTL缓存降auth负载(#195) · ASSET-THUMB资产页缩略图COS webp小图修裂图(#225) · REVERSE作图页提示词反推(图→Gemini→提示词→填框/图生图,补nginx精确路由+并发闸,#220) · CANVAS-NODE节点式生产画布(#223) · LEADGEN-PAGE获客采集按采集量翻页(#227) · SEC-XSS-P1统一转义器+采集页转义(#194) · ARCH-DEBT-P2有界任务队列+固定worker治无界线程(#195) · INSP-CAT-ICON灵感页9分类3D图标 · COLLECT2BANANA转作图丢图修复(#233) · FIX-NAV导航safeUrl放行相对路径(#228回归) · CANVAS-FIX画布布局与shell冲突(菜单不可点/卡顿)修复(#240) · COLLECT-WXCH视频号加密流先解密再存COS(#242) · CH-RETRY视频号详情缺play/decodeKey重试一次(#243) · COS-PRIV-P2私有ACL+预签名+/api/gen/file归属鉴权+dl加鉴权+前端fetch带token→blob加载(#185) · MOTION-RENAME动作模仿→影视级模仿更名+预设/上传三处bug(#249) · NAV-3D-ICON全站金调3D图标(导航+功能页标题头,#244) · COLLECT-DL内容爬取视频下载模式(#254) · SEC-XSS-P2主站CSP响应头(#194阶段2,#255) · CANVAS-UX画布交互优化(拖拽/撤销/连线/复制/缩放,#253) · CANVAS-TPL画布模板+泽龙AI引擎(#259) · CH-COLLECT视频号collect闭环:channels不转存COS+透传decode_key(#261) · HANDOFF-IMG编导转作图带分镜画面描述(#252,#262) · ARCH-DEBT-P3列表接口移除同步外部调用(#195阶段3,#248) · SCRAPER-OBS修xiaotan热重载reload扫.venv致42%CPU+熔断/告警(#241,#247) · FAKE-UI全站假控件三态收口(canvas真节点#223+video影子UI清#270+后台dashboard/cost示例预览横幅#273,#190完结) · HEYGEN-FIX HeyGen错误处理(URLError/日志/密钥自检,#267)+捞回线上中转relay/webp转jpg漂移入git(#258/#192) · COLLECT-FS采集发飞书/入库假控件三态收口(#229,#266) · RESIL-SECRETS密钥分服务隔离与轮换流程+模板(#196子项,#268) · ARCH-DEBT-P4移除content_domain全量core注入改显式import(#195阶段4完成,#271) · FAKE-UI-VIDEO清理video.html影子UI+守卫JS引用(#190video子项,#270) · FB-VID-IMG-VALIDATE视频图片提交阶段魔数校验(PNG/JPG/WebP文件头,假图400不扣点,#60反馈,#319) · CANVAS-POLISH画布节点交互精修(#314) · 端口收敛cupsd631关闭(RESIL#196) · ASSET-VID-RATIO资产库视频放大object-fit:cover→contain不裁剪+居中放大弹窗(#306,#311) · VID-THROUGHPUT视频吞吐:worker3→6(content.env)+单用户pending/running限流max5(预扣点前拒429不扣点),health暴露job_workers/max_user_active_jobs(#304+运维worker) · VIDEO-PLAYER-MODAL视频播放居中9:16蒙层弹窗+懒加载鉴权+pause释放(#292,#302) · SEC-XSS-P3-CORE登录态迁httpOnly Cookie:6后端服务双读(Bearer优先cookie兜底)+auth发hq_session cookie+全站前端__cookie__占位/credentials(#194阶段3步骤1-4,#294) · AUDIO-OWN配音试听/下载归属鉴权补audio_assets表修404(yuelei,#285) · RESIL-POOL泽龙2生图号池多key轮询+失败切换(#196子项,#286) · AUDIO-COS音频产出走COS修复:根因content进程缺COS env(重启已装env,新音频出COS直链已实测)+public_url加可观测(跳过上传/COS未启用日志)(#196相关) · HANDOFF-VID作图转视频带图前端token blob试听下载(#276已含) · AUDIO-PLAY音频试听下载走鉴权blob修/api/gen/file私有401(yuelei反馈,#280) · DRIFT-GUARD部署反漂:哨兵detect比线上vs最新git+ship部署后校验+checkout只读钩子(#192防再漂完成,#278) · RESIL-PORTS端口暴露只读审计脚本(#196子项,#279) · AUDIO-COS-ROOT豆包正式配音转aud_*走COS公开链不再复用voice_preview敏感本地URL(捞回#284根因,#289) · ASSET-PROMPT资产库图片查看/复制完整生成提示词弹窗(数据已存补UI,#298,#300) · IMG-FALSE-FAIL作图误报失败:轮询超时(400→900s)改绿字「生成中请查资产库」+只真error判失败(#317,#331) · VID-REGEN-MODE视频资产重生成还原原模式:记mode(background_file/tryon_mode/model)+按mode跳对应页回填(向下兼容口播)(#325,#333) · CANVAS-FIXES画布6个bug批量修:多参考图限1张+明确提示/storage事件跨标签同步/协作tab收口"即将上线"/循环依赖检测+高亮/非图片onerror校验/图片压缩webp-jpeg+配额提示(#320-324,#327,#334) · ASSET-IMG-CLICK图片点击灯箱放大预览:资产库/作图页最近作品缩略图点击改灯箱大图(左右切换+下载)+banana保留设为参考移入灯箱按钮(#329,#336) · INSP-LANDSCAPE灵感库风景分类扩充:新增10张16:9风景webp封面(beauty_049-058)+灵感数据10条,页面计数48→58(#309,#335) · POINTS-DETAIL点数使用明细:GET /api/gen/points/history(鉴权+jobs表消费+天数/功能筛选+分页)+cloud-shell点数旁明细金调弹窗(时间/功能/消耗/状态/任务ID)(#316,#342) · VID-TRYON-COVER换背景视频补封面:tryon封面image_file改clothes_file or background_file(bg_only用背景图,clothes_only/both用衣服图)+3模式单测(#338,#346) · VID-MOTION-RATIO影视级模仿加画面比例:motion面板加独立比例控件(9:16/16:9/1:1/4:5/5:4,selectedMotionRatio默认9:16)+提交/草稿/回填用之,后端已传aspect_ratio(#345,#349) · VID-GROK-RATIO果肉视频加画面比例:grok面板加独立比例控件(9:16/16:9/1:1/4:5/5:4,selectedGrokRatio默认9:16)+后端generate_xiaole_video收ratio校验合法值透传input.aspect_ratio+单测(非假控件:同generations端点果肉生图已实测aspect_ratio生效)(#345需求,#361) · GROK-RATIO-REVERT果肉视频撤aspect_ratio止血:#361误给果肉/Grok视频input_d塞aspect_ratio但视频模型不支持(HTTP422当前模型类型不支持,今日挂50次),后端撤字段+前端撤假比例控件(FAKE-UI)+测试改断言不发,实测create200恢复。motion(HeyGen)aspect_ratio保留(#367,#368) · VID-GROK-SIZE果肉视频画面比例(用size正确重做,#367教训):Grok视频模型收size不收aspect_ratio,前端传比例标签→后端XIAOLE_RATIO_SIZES映射像素(9:16→720x1280/16:9→1280x720/1:1→1024x1024/4:5→1024x1280/5:4→1280x1024,五个全实测API接受),未知比例默认9:16,单测断言发size不发aspect_ratio。上线查jobs表11:32后aspect_ratio 422=0(#345需求,#375) · MOTION-PRESET-THUMB影视级模仿预设缩略图裁切(#372,P2):方案A纯CSS(网格auto-fit minmax128+卡片min-height:auto+缩略图aspect-ratio:9/16 height:auto),竖版不再被裁(#380) · SEC-XSS-P3-CLEANUP登录态迁httpOnly Cookie收尾(步骤5):登录/注册不再返明文token+全站前端撤localStorage hq_token读取(纯__cookie__+credentials cookie鉴权)+auth-guard去bearer兜底+新增cookie登录无token body回归测试(#194阶段3完成,#363) · IMG-XIAOLE作图页第4引擎泽龙AI(偏不稳)换果肉生图(xiaolevideo.cn gpt-image-2,同果肉视频账号,写实稳定8点):image.py新增_gen_image_xiaole(创建→轮询→落盘,复用XIAOLEVIDEO凭据)+banana.html引擎卡/COSTBASE/submit替换,zelong后端保留可逆(#350) · IMG-XIAOLE-TIMEOUT果肉生图轮询死线180→300s(实测239s顶死线易误判出图超时,前端banana容忍900s余量足) · VID-PLAY-SLOW-P1 CDN加速已上线:kongli本人配置video.huangquechuanmei.com(阿里云域名DNS+腾讯云CDN跨云,源站=COS桶huangque-media-1435693839,免费证书已签发绑定)+服务器COS_DOMAIN env已设+huangque-content/imggen-api已重启验证,实测HTTPS拉取200(#339 P1完结,P0/P2此前已上线,全部完成) · VID-FRAME-COVER视频首帧封面(#365,#378):ffmpeg -ss1截首帧作封面统一各模式+public_url上COS(实测封面CDN链HTTP200 image/jpeg可显示);⚠️历史修补脚本fix_video_first_frame_covers.py查错DB(jobs.db应content_jobs.db)找0候选,历史回填待修DB路径 · POLL-FAILED-STATUS轮询漏判failed修复:video/audio/canvas/leads/script 5页pollJob加||d.status===failed,失败任务及时显示失败不再卡「生成中」30分钟(#391,#396) · ASR-TRANSCRIPT-SLOW口播提取慢改善:ASR并发2→1防挤+OpenAI转写75s显式超时(原300s)+分段耗时日志+前端提示改诚实,实测发现detail()解析也慢27s凭新日志观察(#386,#393) · PERF-CLOUD-BG工作台背景动画性能(#389 P0):cloud.js加visibilitychange隐藏暂停rAF(标签页切走停省CPU/GPU)+mousemove 50ms节流passive+FPS帽~30fps+粒子76→64(导航SPA化归GROW-FRONTEND#203)(#389,#401) · MINIPROGRAM-AUTH小程序专属登录/注册接口(#11最高优先级第一阶段):新增/api/auth/miniprogram-login+miniprogram-register响应体带token供小程序走Bearer鉴权,网站原login/register不动仍只走cookie(SEC-XSS-P3-CLEANUP#363不受影响),线上实测路由生效(#398) · NEWUSER-TRIAL-POINTS新用户注册赠送16点试用额度(#11小程序MVP联调发现points=0直接卡住无法生成):网站/小程序两个注册入口统一改动,管理员CLI建号不受影响,线上实测注册返回points:16(#404) · HEYGEN-DIRECT数字人口播+影视级模仿双直连HeyGen甩开泽龙(kongli决策):口播#405(实测70秒,upload.heygen.com talking_photo/asset+v2/video/generate+代理下载);motion#410(泽龙即HeyGen v3 API同构转发同一账号,直连只换base+mihomo代理,v3辅助函数加direct开关,成功素材实测243秒vs泽龙同素材753秒,直连轮询死线390s);两模式均直连优先失败自动回退泽龙,reaper分模式超时口播300s/motion480s(kongli拍板5min/8min)。⚠️遗留:HeyGen计费待核(900额度出片扣0,kongli控制台核实);motion静默failed为素材相关(同素材泽龙也失败,非直连问题) · VID-REGEN-MOTION影视级模仿重生成回填修复(#415,#473):mode为空误跳口播改按有reference_video推断motion+fetchFilePrefill失败toast提示 · NAV-BELL顶部通知铃铛接功能(#413,#466):点击跳获客/资产页+红点默认隐藏待接真实通知+aria键盘可达 · GROW-CRM-INTENT获客CRM阶段3意图AI分级(#201子项,#456):线索按意图关键词/AI分级打标,GROW-CRM主体收口(剩一键推客户群待#197飞书) · SUB-POS口播字幕位置5档(#478):顶部/偏上/中央/偏下/底部,_SUB_POSITIONS(ASS Alignment+MarginV)前端5按钮默认底部,已部署漂移bless · SUB-WRAP字幕长句超视频边界修复(#481):单行max_chars改按可用宽度(w-2*边距)÷中文字宽(fs*1.05)算(原w/fs*0.62算多1.6倍字致溢出)+_wrap_cn多行折(≤4)尾行兜底截断,已部署 · SETTINGS-PWD设置页改密入口安全化(#480):删假token Bearer __cookie__改credentials same-origin带真cookie+apiFetch统一401处理+no-store,改密改卡片为弹窗(Esc/遮罩关+focus+aria),已部署/var/www漂移bless · LINE2-WAVESPEED线路二WaveSpeed后端骨架(#457):新增wavespeed.py(generate_motion wan-2.2/animate+generate_tryon outfit-tryon,素材转存COS拿直链→提交轮询下载入库,返回结构对齐线路一),gen_video按line参数分流(前端默认线路二·极速约2min),video.html加线路tabs,已上线。⚠️遗留:换装gen_tryon分流未接入gen_tryon(骨架待续)/COS输入素材wavespeed-input/无清理累积 · PASTE-UPLOAD换装/动作模仿上传槽支持粘贴图片Ctrl/⌘V(#483):PASTE_SLOTS各上传框悬停粘贴剪贴板图合成File塞input触发change,换装人物框仅线路二收图时可粘,已上线 · TRYON-LINE-LABEL换装页线路标签点明换背景走线路一(#482):换背景之前被藏在默认线路二后,标签明示"换背景走线路一",已上线 · SHIP-PORTABLE ship部署脚本去开发机硬依赖+rsync缺失回退(#479):ship不再硬依赖特定开发机,rsync不可用时补传输回退(与本机无dapeng-key/rsync环境呼应) · POINTS-AUDIT任务级扣点/退点写points_audit流水(#477):auth_server/core/points/imggen/leadgen五服务+后台index接扣退点审计流水,12文件含points_audit与各CAS回归测试,7文件线上==git已部署 · EGRESS-CHAIN作图三引擎出境优先级链(#491):content_domains/egress.py出境链VPS Reality隧道→mihomo(法兰克福)→heygen兜底(ProxyHandler({})显式绕过进程级代理保证兜底直连),imggen(nb2/pro)+image.py(gpt)改走egress.post_json,泽龙系不动,只通道级失败降级/HTTP200直返,安全默认(未配EGRESS_*=老行为零风险),14例单测。已合并部署+搭VPS xray Reality隧道启用(阿龙专属154节点凭据写/home/ubuntu/egress/xray-client.json 600不进git,实测穿墙openai401/gemini403 vs国内直连000,egress链vps→mihomo→heygen激活,两服务进程读到env)。⚠️遗留:unit的StartLimitIntervalSec误在[Service]段被systemd忽略(不影响运行,Restart=always仍生效),待小修 · MOTION-FIX影视级模仿线路一融合过度+线路二抽帧(#500/#501,#502):线路一HeyGen prompt强化(ONLY follow reference for body movement+CRITICAL保持avatar本人identity/face/skin/clothing+Do NOT copy参考视频人外观)防融合过度;线路二WaveSpeed最低分辨率480p→720p防抽帧,已部署 · NOTIFY-CENTER全站通知中心(#413完整实现,#503):顶栏铃铛升级右侧抽屉通知中心(4类tab全部/任务/点数/系统),复用/api/gen/points/history构造任务+点数通知,未读badge/单条+全部已读(localStorage按username隔离slice300)/点击跳对应页或开点数弹窗,escapeHtml/escapeAttr全转义防XSS,Esc/Enter键盘可达+移动端,notifyBtn由跳leads升级为开面板(非回退),15文件已部署漂移bless · IMG-FAILRATE作图失败率base64容错+瞬时退避重试(看板#6,#505):image.py新增_clean_b64(剥data:前缀/去空白换行/补padding,修剪贴板粘贴带换行致Incorrect padding整单失败退点)+_retry(只重试429/5xx/URLError/Timeout等瞬时错,4xx内容审核不重试,退避1.5/3s最多2次在reaper900s内)接zelong单发+zelong2号池每号,gpt/nb2/pro已由#491三通道兜住不叠加防双计费,14例单测,已部署 · TUTORIAL-VIDEOS教程区正式教学视频上线(#498,审核AI重建):9个教程mp4(80MB)传COS走CDN直链(video.huangquechuanmei.com/huangque/tutorials/)git内0个mp4,新建tutorials.html教程页+cloud-shell.js加「教程」导航项(只叠加不回退#503/#475),tutorials.html补esc()/safeUrl()防XSS,webp封面随git,重跑戳f90e1160。#523清理旧占位overview-1(meiye 38s假标3:20)。已部署26文件漂移bless线上9视频206可播 · VID-MOTION-DURATION影视级模仿按参考视频原时长(#476,#499):ffprobe读真实时长_probe_video_duration+_motion_reference_duration按线路一HeyGen限5-30/线路二WaveSpeed限120超限明确提示,向上取整,合并时解语义冲突保留main默认线路二+#502prompt强化,已部署 · MOTION-FIX-2线路一融合过度+线路二抽帧(#500/#501,#502):prompt强化ONLY body movement+CRITICAL保avatar本人+Do NOT copy参考人;线路二最低480p→720p防抽帧,已部署 · EGRESS-PRIMARY-TIMEOUT出境首选档超时可单独放宽(#515):EGRESS_PRIMARY_TIMEOUT只作用VPS首选档默认回落零风险,三档和<reaper900s有测试守,已部署 · GROW-FRONTEND-A11Y共享外壳可访问性首切片(#203子项,#475):burger加aria-controls/expanded+setNavOpen,登录tab div→button role=tab+aria-selected,退出/登录卡/注册/团队div|span→button补border0/bg透明/font继承,rebase保留#503通知中心零回退,已部署15文件 · ASSET-COLLECT-FIX资产库采集视频预览+滚动条(#488/#492,#520):采集视频a.href直开改「预览视频」弹窗,永久CDN直播/非永久走/api/gen/dl带登录态转blob(401跳登录/502已过期提示),金色滚动条,已部署 · EDITOR-SCRIPT-ACTIONS编导脚本导出+历史(#493,#522):导出UTF-8 BOM TXT下载+文件名过滤非法字符,历史从/api/gen/assets?kind=copy读带scenes资产点击恢复分镜,转作图/口播handoff加固+esc/escAttr,已部署 · DEPLOY-HARDEN部署链硬化(#510/#514/#513,#517):ship整目录--delete同步content_domains防漏文件+重启前生产python3 import content_api冒烟防崩溃循环+健康检查真gate(非2xx exit1+等active+诊断);xiaotan单元加network-online/重启限流/journal(ExecStart不变),写文件+daemon-reload零重启小探。⚠️审核AI补:content_domains被content+imggen-api+admin三服务共用,部署后三个都要重启(曾漏admin已补) · VID-COVER-BACKFILL历史视频首帧封面回填(#365,#506):回填脚本修连错库改读audio_assets.db+只读URI+路径越界防护+dry-run真不写+幂等,tang生产执行(备份integrity ok/dry-run56/56/三批fixed=56 skipped=0),#365已关

## 🧭 自助领取流程（agent 自己领，靠「组锁」原子防冲突）

**认领的唯一凭据 = 组锁分支 `lock/<组>`**。创建 git 分支(ref)是**原子操作**：两个 agent 同时抢同一个组锁，只有一个能 push 成功，另一个报错——所以**抢到锁 = 合法认领，抢不到 = 该组已被占**。看板的 🔵 状态只是给人看的镜像，由维护者同步。

**步骤：**
1. 看板挑一个 **🟢 可领** 任务，看它的**冲突组**。
2. **抢组锁**（先列出你 PR **实际要改的所有文件**、逐一归组，对**每个**组 G 逐个抢）：
   ```bash
   git fetch origin                                  # 先同步，确保基于最新 main
   git ls-remote origin refs/heads/lock/<G>          # 先看有没有被占(有输出=被占)
   git push origin origin/main:refs/heads/lock/<G>   # 抢锁：成功=抢到；报错(rejected/exists)=已被别人占
   ```
   - **跨组/多文件任务**要把**每个涉及的组都抢到**；任一抢不到 → 把已抢到的用 `git push origin --delete lock/<组>` 退掉、换别的任务或等。
   - 改 `cloud-shell.js` 必占 **shell 排他组**，且当时不能有任何前端页任务在做（见冲突组表 ⚠️）。
3. **抢到锁后立刻更新本看板该行**：状态改 **🔵 进行中** + 填**领取人**（一并提交，别等维护者——看板要实时反映你已认领）。
4. **基于最新 main 开分支**：`git fetch origin && git checkout -b codex/<任务ID> origin/main`。严格按对应《工单》+《开发手册》/§0 开发（走 PR、只走 ship、禁止直改服务器、署名 kongli）。
5. **交 PR 前必须 rebase 到最新 main（防旧分支回退近期工作，强制！）**：
   ```bash
   git fetch origin
   git rebase origin/main             # 拉平到最新主干，解决所有冲突
   git diff origin/main...HEAD --stat # 自检①：改的文件必须 ⊆ 你锁的组，多一个都不行
   git diff origin/main...HEAD        # 自检②：确认没夹带删除/回退别人近期合并的功能
   ```
   - 这是**防冲突的最后兜底**：哪怕锁漏了，rebase 也会把主干新改动合进来、把冲突暴露出来让你解决。**没 rebase 干净、diff 里含别人的改动 → 别交 PR**（会被直接打回，#240/#244 都栽在没 rebase）。
6. 开 PR（**别开成 draft，做完就 Ready**），把看板该行改成 **🟡 待审**。PR 正文注明**改了哪些文件、锁了哪些组**，方便审核核对。
   - 🚫🚫 **开完 PR 就停下，绝对不要自己合并/merge！** 合并 + 部署 + 上线由**审核 AI** 统一把关。你自己合会**绕过审核、且不会被部署到线上**，只会造成 git 与线上分叉。开完 PR 就等审。
7. **审核 AI 合并上线后**：释放你占的每个组锁 `git push origin --delete lock/<G>`。

> **看板维护分工**：**领取的 agent** 负责改自己那行为 🔵进行中(认领时)/🟡待审(交PR时)；**审核 AI** 负责合并后标 ✅、移入已完成、校正对齐、清理卡死的锁。
>
> **审核 AI 合并前必查三关（任一不过 → 打回，不合并）**：① **分支基于最新 main**（`mergeable_state` 非 behind/dirty；落后就让其 rebase 或用 update-branch 重跑 CI）；② **改动文件 ⊆ 声明锁的组**（没锁的组文件被改 = 越界）；③ **不回退近期合并**（比对关键函数/文件是否被旧版覆盖，如前端安全加载、近期 bugfix）。这三关就是防「旧分支合并把已上线的东西冲掉」。

> **一句话规则：只领 🟢 且能抢到全部组锁的任务；抢不到就换一个或等。** 组锁名 = 组字母：`lock/A`(后端核心) `lock/B`(视频页) `lock/C`(资产页) `lock/D`(认证) `lock/E-<页名>`(其它页) —— F 独立组不用锁。
>
> 查当前被占的组：`git ls-remote origin 'refs/heads/lock/*'`

---

## ➕ 加新任务 / 反馈转任务

- 新需求或 Issue #60 的反馈 → 由 kongli/审核 AI 出《工单》入 `docs/`，在本看板加一行（标好**冲突组**和状态）。
- 加行时**必须标冲突组**（按它要改的文件归组），否则并行会撞车。

---

> 配套：《开发手册-黄雀网站.md》(怎么改)、《工单-其他agent实施》§0(公共上下文)、Issue #60(反馈台)。
 · JOB-QUEUE-SPLIT任务队列分快慢池:慢池(video/tryon/xiaole_video)6worker+快池(图片/音频/文案)3worker(CONTENT_FAST_JOB_WORKERS),视频不再堵死本该几十秒的作图/配音(证据#1014作图排4视频后等31.5min)(#357) · VID-REGEN-CLONE数字人口播「重新生成」参数回填(#395,#416):assets重生成payload口播mode别名归一(talking_text/text2video→text,talking_audio/audio2video→audio)+voice/形象图回退链;video页applyTemplateVoice支持voice_key/provider_voice/display_name匹配+_applyPrefillVoice仅匹配成功才清空(修音色列表未加载完回填丢失竞态),已部署+漂移bless+线上200 · CANVAS-SIDENAV画布右侧图标导航+节点管理/资产浏览/JPG导出面板+模板与更多菜单移入右栏(LU-003 #397);⚠️审核方补回该重写误删的近期功能后才合:tok()恢复__cookie__(SEC-XSS-P3 #363回归)/图片魔数校验validateImageFile+rejectImageFile(#327)/循环依赖检测detectCycle/pollJob补failed(#396)+修cloud-shell戳,已部署+漂移bless+线上200实测 · SCRAPER-SPEED采集管线提速(#412,#423):超时全env可配(搜索静态480→180/青果210→120/视频420→180)+登录态热缓存(cookie mtime+TTL900s跳Chromium预热省~30-45s)+抽run_mediacrawler进程组防孤儿chromium+handler补捕TimeoutExpired(修超时未捕获崩溃+部分数据可用就用),已部署5个leadgen-worker@1-5(user systemd)滚动重启+md5==git · INSP-EXPAND灵感库扩充58→272张(#409,#421):18分类×15(含9新美业垂类:皮肤管理/医美科普/门店海报/体验卡/招商/日签/团购/数字人首帧/客户案例),272条全含prompt+有效webp(0孤儿0非法),inspiration.html计数改动态(caseTotal读DATA.length)+新分类映射3D图标+chips动态,已部署tar解包到/var/www(58→272实测)+漂移bless+线上200 · ASSET-BATCH资产库批量下载/删除(#408,#426):新增asset_batch.py,批量ZIP下载(本地写文件/远端COS写URL清单避免拉大视频占内存)+批量软删除(deleted=1预留回收站),全程verify(_token())鉴权+四类资产username域查询(越权取不到)+fp.resolve().relative_to(OUT_DIR)防路径穿越+批量限120,前端多选/全选,已部署+重启content+线上实测无token两端点均401+漂移bless · VID-DL-404豆姐成片下载404修复(#519):中转失败时兜底直连取片,修下载404,已上线 · IMG-TEMPLATE-RATIO-SYNC作图模板↔比例双向同步(#495,#526):banana.html新增syncTemplateForRatio(按比例反查匹配模板)+selectRatio(next,syncTemplate)双向联动——选模板自动切比例、切比例高亮对应模板,syncTemplate=false防递归回环,4条前端回归测试,已部署漂移bless · COLLECT-FAILED-STATE内容爬取failed终态收尾(#458子项,#524):collect主任务轮询与提取口播文案轮询均由`d.status==='error'`改判`error||failed`,后端失败即结束等待并显示退点提示(不再空等到600s);超时600s/reaper collect 1200s宽限先前commit已完成,video轮询failed由#396完成,2条回归测试,已部署 · SHIP-RESTART-GATE ship重启闸门(#521,审核方rebase只留其独有增量):smoke_import从systemd ExecStart/WorkingDirectory反查入口逐服务import冒烟(替换#517硬编码只测content)+check_restart_effective两条后置断言(ActiveEnterTimestamp>服务器时钟T0证明restart真发生;无EnvironmentFile比启动新证明配置真加载)。审核方发现#517的fake ssh mock按命令行匹配、而smoke_import从stdin喂脚本→冒烟段被静默放行(测试假过),增强mock并补2项行为测试 · ASSET-DOC-VIEW资产库文案/名单查看(#486/#487/#494,#527):名单弹窗全程createElement+textContent(PII与采集UGC,天然免疫XSS),外链URL协议白名单挡javascript:+rel=noopener,后端_LEAD_META_FIELDS白名单只投影15字段(测试断言多余字段不进),_copy_body把scenes格式化解决文案打不开,历史资产按job_id回源不回填(避开#518膨胀),已部署 · SEEDREAM-ENGINE Seedream生图引擎(火山方舟Ark,#531):新增标准/Pro两档(点数同gpt:标8/高12),文生图+图生图,proxy=False绕过指向法兰克福的进程级HTTPS_PROXY直连国内火山,response_format=url避开b64_json的IncompleteRead,输出显式png(不指定默认吐JPEG),SensitiveContent映射友好错误走退点。⚠️审核方拦下重复计费:原实现把非幂等出图POST包进通用_retry(会重试5xx/超时,那时Ark很可能已出图并计费)→改_seedream_post只重试429(限流确定未出图),补5项守卫测试。ARK_API_KEY已配并实测(GET /models 200/0.36s直连) · SEEDREAM-PRO-CAP Seedream Pro高清必失败(#534):_seedream_size只有像素下限没上限,高清档一律取9.4M px而Pro上限仅4624220→必400(线上#1522/1523/1524三单,均已退点);新增SEEDREAM_MAX_PIXELS{std:16777216,pro:4624220}+variant透传+双向收敛夹逼,未知型号取最保守上限。审核方复算16种组合(4比例×2画质×2型号)全在窗口内、比例漂移≤0.69%、1~2步收敛,修复后pro hd 9:16=1600x2848与线上实测标200的尺寸精确吻合;并把参考图魔数校验从引擎内(扣点后)提到validate_image_payload(扣点前,所有引擎受益且不扣点),修正两个用假字节当蒙版的测试(会先被格式校验拦下,测不到"引擎没有inpaint"的本意) · IMG-PAYLOAD-VALIDATE图生图扣点前校验(#535):core.py在cost_of()之前调validate_image_payload,非法/超大base64直接400不扣点,前端参考图收敛(现网本就压1024,本PR提到1280反而提质),缓解#518的jobs.payload膨胀。⚠️审核方修三处:①原实现b64decode(validate=True)会拒绝「剪贴板粘贴带中间换行」(#483 Ctrl+V)与「反推回填缺padding」——这些是合法前端产物,#505专门用_clean_b64修过,回归后功能彻底不可用→改先清洗再校验(反证:原实现对两种脏输入均400);②参考图压JPEG但gpt图生图/局部修改都走/v1/images/edits且_multipart硬编码Content-Type:image/png,OpenAI要求真PNG→改回PNG,体积靠尺寸收敛;③canvasToRefDataUrl二次缩放时cv=small只改函数内绑定,调用方仍返回外层尺寸→dataUrl与refW/refH不符,inpaint画布复用refW会让mask错位→合并为shrinkToBudget尺寸随dataUrl回传。16项新测试守住"清洗在前、校验在后" · IMG-DEADLINE-600果肉/泽龙2出图死线300→600s(#533):用户15544499908近10次图片失败中6次「出图超时」全是xiaole hd图生图,耗时302~319s正卡在300s死线上(全站近7天:成功中位193s最大446s,失败中位315s)。xiaole轮询死线→XIAOLE_IMG_DEADLINE(600);zelong2**不是**简单放宽单次timeout——号池遍历N个账号×_retry 2次,最坏N×2×timeout会冲破reaper KIND_GRACE[image]=900s(改前就有此隐患)→改为总死线ZELONG2_DEADLINE(600),每次按剩余预算当timeout、预算耗尽不再试下一个号;core._post加timeout参数(默认300不改其它引擎)。7项测试守住"两档死线都<reaper 900s" · NOTIFY-TASK-FOCUS通知中心「已完成」跳资产库定位高亮(#530):原PR只在href拼?task=却无任何页面消费(点击行为与改前完全一致,却bump全站15页缓存戳)→审核方补消费方:已完成任务跳assets.html?cat=&task=,资产库五类卡片经withJob挂data-job=job_id(与通知task_id同源),render后focusJobCard滚动定位+金调高亮2.6s、一次性消费、找不到静默返回、尊重prefers-reduced-motion;失败/进行中仍跳生成页。task_id补encodeURIComponent(含&?#不再串参),空id不产生空?task=。15项测试 · TUTORIAL-OVERVIEW-FLOW教程区「从获客到成交」正片(#532+#536):原PR把source改mp4指向未上传的CDN对象——实测返回HTTP 200+Content-Length:0+Content-Type:application/xml(COS NoSuchKey被CDN原样透传,状态码不是404,前端失败提示未必触发)→先只合安全部分(封面webp入git+时长改04:34,source保持coming);kongli提供文件后审核方传COS(24173732字节,md5与源文件一致),CDN复验4/4(200/Content-Length一致/video/mp4/Range 206)才启用source=mp4。教程区10个视频全走CDN,git内0 mp4 · WORKBENCH-THEME工作台浅色/深色主题+设置页重整(#529):theme-init.js在head同步执行无FOUC+跨标签storage同步,theme.css用:root变量+174处[data-theme=light]覆盖(默认暗色不受影响),通知偏好按账号存(taskProgress默认false,进行中任务不再弹通知),设置页加头像颜色(白名单查表不可注入style)/可折叠通知偏好/会话安全入口,移除hqBizSwitch「仙颜美容·示例」假控件,icon stroke改currentColor适配主题。⚠️审核方补:theme.css/theme-init.js写的是手写死值?v=1而stamp_assets.py的STAMP_RE只认cloud-shell.js→第一次调主题样式戳仍是v=1、浏览器继续吃缓存→泛化为ASSETS多资源注册表(各按自身内容hash独立打戳,cloud-shell.js保持required,theme为页面级不强制),8项测试。第三关核验#503/#475/#498/#530/#520/#527/#480全部保留,settings.html的accountKey()与cloud-shell.js的accountStorageKey()生成同一个localStorage key
