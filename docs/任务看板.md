# 黄雀 · 任务看板

> **单一事实源：谁能领什么、谁在做什么、哪些会撞车。** 派活/领活前先看这里。
> 维护：kongli（+ 审核 AI）随任务推进更新状态。最后更新：2026-07-04

---

## 🚦 核心规则：按「冲突组」领，同组同时只能 1 个在做

两个任务只要**改同一个文件**就会撞车（合并冲突/互相覆盖）。所以按文件把任务分成**冲突组**——**同一组里，同一时刻只允许一个任务处于「🔵 进行中」**。不同组的任务可以并行。

| 冲突组 | 涉及文件 | 规则 |
|---|---|---|
| **A · 后端核心** | `server/content_api.py`、`server/content_domains/**` | 组内串行（一次一个）|
| **B · 视频页** | `site/workbench/video.html` | 组内串行 |
| **C · 资产页** | `site/workbench/assets.html` | 组内串行 |
| **D · 认证** | `server/auth_server.py` | 组内串行 |
| **E · 其它前端页** | 某个具体 `workbench/*.html` | 按页面，改同一页才冲突 |
| **F · 独立** | `ci.yml`、`docs/**`、字体、脚本等 | 基本不冲突，可随意并行 |

> ⚠️ 有的任务**跨组**（比如"批量生成"同时改 content_api 和 video.html → 占 A+B 两组）。跨组任务要等它占的**所有组**都空闲才能领，做的时候也**锁住这几组**。

---

## 📋 任务列表

**状态图例**：🟢 可领 ｜ 🔵 进行中 ｜ 🟡 待审(PR开着) ｜ ✅ 已完成 ｜ ⏸️ 阻塞(等素材/决定)

| ID | 任务 | 冲突组 | 状态 | 领取人 | 工单 |
|---|---|---|---|---|---|
| FB1 | 配音点数 4→10 | A | 🟢 可领 | — | `工单-FB1-配音点数改10-20260704.md` |
| F5 | 批量生成 | A+B | ⏸️ 待出工单 | — | 待写 |
| F4-BGM | 视频背景音乐 | A+B | ⏸️ 等 BGM 素材 | — | `工单-F4-字幕与BGM`（Phase2）|
| F2+ | 参考动作库加素材 | B | ⏸️ 等素材 | — | `工单-F2-参考动作库`（清单驱动，丢文件即可）|
| — | 收藏/标签(资产&视频)| A+C | ⏸️ 待出工单 | — | 待写（需后端持久化）|

### 已完成（近期，供参考，别重复做）
F1 形象库 · F2 参考动作 · F3 进度ETA · F4 字幕 · F6 模板库 · F7 资产增强 · T1/T2/T4/T5/T6/T7 · P1-5 缓存戳 · T8 content_api 拆分

---

## 🧭 领取流程（agent / 派活人照做）

1. **看本看板**：挑一个 **🟢 可领** 的任务。
2. **查冲突**：确认它的**冲突组里没有 🔵 进行中** 的任务（跨组任务要所有组都空）。有冲突就先别领，换一个或等。
3. **认领**：把该行状态改成 **🔵 进行中**、填**领取人**，并**开分支** `codex/<任务ID>`（分支存在 = 已被认领的可见信号）。
4. **开发**：严格按对应《工单》+《开发手册》/§0（走分支 PR、只走 ship、禁止直改服务器、署名 kongli）。
5. **提交**：开 PR（**别开成 draft**，做完就 Ready），状态改 **🟡 待审**。
6. **合并上线后**：审核方把状态改 **✅ 已完成**，释放其冲突组。

> 认领竞争：本团队由 kongli 统一派活——**同一个冲突组，kongli 一次只派给一个 agent**，从源头避免抢同一任务。agent 领到后按上面走。

---

## ➕ 加新任务 / 反馈转任务

- 新需求或 Issue #60 的反馈 → 由 kongli/审核 AI 出《工单》入 `docs/`，在本看板加一行（标好**冲突组**和状态）。
- 加行时**必须标冲突组**（按它要改的文件归组），否则并行会撞车。

---

> 配套：《开发手册-黄雀网站.md》(怎么改)、《工单-其他agent实施》§0(公共上下文)、Issue #60(反馈台)。
