# PR #1309 UI evidence

These screenshots were captured from isolated local static checkouts; no production or test server was accessed.

- `before-main-1165d66b.png`: latest reviewed base `1165d66bffacf439d00c7142bfcc16ad7d2b8b4b`, served from `http://127.0.0.1:8010/`.
- `after-precision-top.png`: PR candidate after synchronizing that base, served from `http://127.0.0.1:8011/`; shows the new `真人视频 Precision` tab and upload entry.
- `after-precision-panel-lower.png`: the same candidate and session, scrolled within the Precision panel; shows the voice-extraction/clone flow, edit templates, consent gate, and progress stages.

Because this was a static, unauthenticated local preview, API-backed controls remain disabled and no provider request or paid job was submitted.
