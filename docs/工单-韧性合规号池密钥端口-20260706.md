# 工单 · 韧性/合规：号池冗余 + 密钥拆分 + 端口收敛（#196 P1）

> 供其他 agent 独立执行。先读《工单-其他agent实施》§0 +《任务看板》。
> **冲突组：F/运维（content.env、防火墙、chatgpt2api 主机）+ A（若 image.py 加号池轮询）** —— 运维为主。
> ⚠️⚠️ **本单多处需 kongli 先拍板/提供资源**，不可擅自动。尤其端口收敛涉及**服务器上的其他系统**(dify/xiaotan/zhipu-proxy/cups/openclaw)——审核 AI 已核实黄雀自身服务全监听 127.0.0.1、未暴露，**这些暴露端口不属黄雀、绝不擅自关**。

## 问题（#196）
1. **泽龙2 号池单点**：chatgpt2api 仅 1 个 ChatGPT Pro 账号(`/opt/chatgpt2api-image/data/accounts.json`)在扛 → 单点故障 + 封号/ToS 风险，一挂 zelong2 生图整链废。
2. **密钥集中**：`content.env` 存全渠道密钥(OpenAI/Gemini/豆包/泽龙/TikHub/HeyGen/COS)，单机泄露=全爆炸半径。
3. **0.0.0.0 端口暴露**：nginx zhipu-proxy:8099、python zhipu-proxy:8102(开放 LLM 反代)、cups:631、dify:3001/3002/5002/5003、xiaotan:8501 等。

## 分项（每项标注决策/归属）
### 1. 号池冗余（需 kongli 提供备用账号）
- image.py 泽龙2 渠道加**多账号/多 key 轮询 + 失败切换**；chatgpt2api 主机冷备或多实例。
- ⏸️ **阻塞点**：需 kongli 提供**备用 ChatGPT Pro 账号/key**，否则只能做"轮询框架"待填。

### 2. 密钥分服务隔离 + 可轮换（可做）
- 按服务拆 env(参照 leadgen 的 `/etc/leadgen-secrets.env` + drop-in 范式)，各服务只读自己需要的密钥、600 权限。
- 建立**轮换流程**文档(哪些 key 换了要同步哪里)。清理源码堆积的 `.bak`。
- 注：这是运维改造，改 systemd EnvironmentFile 拆分，逐服务灰度、别一次改崩。

### 3. 端口收敛（需 kongli 逐个确认，审核 AI 不擅动）
- 🔴 **决策点**：下列 0.0.0.0 端口**不属黄雀**，需 kongli 确认各自用途与是否收敛：
  - zhipu-proxy 8099(nginx)/8102(python 开放 LLM 反代)、cups:631、dify 3001/3002/5002/5003、xiaotan:8501、openclaw 1890-1893/18789。
- 建议：防火墙(安全组/ufw)把非必要端口收敛到必要来源；两个开放 LLM 反代(8099/8102)加认证或关停。
- 黄雀自身服务(auth8095/content8096/dl8097/admin8098/leadgen8100/imggen8101)已全 127.0.0.1，无需动。

## 验收标准
1. 生图渠道有冗余(多账号轮询 + 失败切换)——待 kongli 给备用账号。
2. 密钥按服务隔离、600、有轮换流程；无源码 .bak 堆积。
3. 对外仅暴露必要端口——按 kongli 确认的清单收敛。

## 部署与验证
- 密钥拆分：改 systemd drop-in/EnvironmentFile，逐服务重启验证 health。
- 端口：防火墙规则，收敛后从外部验证不可达、内部功能正常。

> 本工单只做规划，未改动任何文件。**号池待备用账号、端口收敛待 kongli 确认清单**；密钥拆分可先做。
