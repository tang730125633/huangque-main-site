# 端口暴露审计报告（RESIL #196 · 2026-07-07）

> 只读审计，**未改动任何配置**（按 kongli 决策：先只审计不动，线下核对后再收敛）。
> 方法：`ss -lntp` + `docker ps` + nginx sites 比对。防火墙 **ufw = inactive**，当前全靠**云安全组**兜底。

## ⚠️ 前提：服务器是共享机
这台 `129.204.166.13` 同时跑着 **3 拨东西**：
1. **黄雀**（本项目）：auth/content/admin/leadgen/imggen 等
2. **openclaw**（另一个 AI agent 沙箱项目，一堆 `openclaw-sbx-agent-*` docker 容器）—— **不是黄雀的**
3. **code-server**（VS Code 远程，`code-*` 进程，已在 127.0.0.1）

## ✅ 黄雀核心业务服务：已收敛好（无需动）
全部监听 **127.0.0.1 内网**，仅经 nginx 反代对外：
`8095 auth · 8096 content · 8097/8098 admin · 8100 leadgen · 8101 imggen · 8091/8092 uvicorn`

## 🌐 暴露在 0.0.0.0（公网可达）的端口

### A. 正常必需（保留）
| 端口 | 服务 | 说明 |
|---|---|---|
| 22 | sshd | SSH 运维必需 |
| 80 / 443 | nginx | 主站 HTTP/HTTPS |

### B. 黄雀相关，建议收敛
| 端口 | 服务 | 建议 |
|---|---|---|
| **8501** | python 小探深采（xiaotan） | ⚠️ 敏感深采服务**裸暴露公网**。建议：加鉴权 / 绑 127.0.0.1 走 nginx 反代 / 或云安全组只放行内网。**优先** |
| 8090 / 8099 | nginx（leadgen / zhipu-proxy 的额外端口） | 确认是否需要公网直连；若只内部用，收到内网或加安全组限制 |
| 8102 | python3（用途待确认） | 核对是哪个黄雀服务、是否该内网化 |

### C. 系统服务，明确该关
| 端口 | 服务 | 建议 |
|---|---|---|
| **631** | **cupsd（打印服务）** | ⚠️ **零理由公网暴露**。建议 `sudo systemctl disable --now cups cups-browsed` 或绑 localhost。**最安全的明确收敛项** |

### D. openclaw（另一项目，非黄雀，仅告知）
| 端口 | 服务 |
|---|---|
| 1890 / 1891 / 1892 | openclaw agent |
| 1893 / 18789 | openclaw MainThread |
| 5002 / 5003 / 3001 / 3002 | openclaw docker 容器 |

→ 这些是 **openclaw 项目**暴露的，不归黄雀改。但既在同机公网暴露，**建议知会 openclaw 负责人**评估是否收敛。

## 🔑 关键风险：ufw 未启用
- 现在**没有主机防火墙**，所有 0.0.0.0 端口能否被公网访问**完全取决于云安全组规则**。
- 建议：① 核对云安全组是否只放行 22/80/443（其余拒绝）；② 或启用 ufw 白名单（**改前务必先放行 22 SSH，别把自己锁外面**）。

## 建议收敛优先级（供决策）
1. **P0**：`cupsd:631` 关掉（打印服务，零风险收益）。
2. **P0**：`8501 小探` 加鉴权或内网化（敏感深采）。
3. **P1**：核对云安全组 / 启 ufw 白名单（22/80/443）。
4. **P2**：8090/8099/8102 逐个确认用途后决定。
5. **告知**：openclaw 的一堆端口转给其负责人。

> 本报告只审计、未改配置。收敛动作需 kongli 决策后单独执行（改动防火墙/端口属高危，务必先保住 SSH 22）。
