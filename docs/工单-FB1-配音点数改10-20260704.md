# 工单 · FB1 配音消耗点数 4 → 10（来自 Issue #60 反馈）

> 供其他 agent 独立执行。开工前先读《工单-其他agent实施-20260703》§0 公共上下文
> （仓库/分支PR/ship/DESIGN.md/漂移哨兵/**禁止服务器直改**）。
> 反馈来源：Issue #60，提交人 LU-003（账号 qilin），类型 优化。

## 目标
把音频模块「生成配音」（TTS）的消耗点数**从 4 点改为 10 点**，**前端展示与后端扣点保持一致**。

## 改动点（已定位，就两处）
1. **后端** `server/content_api.py`：`COST` 字典里 `"audio": 4` → `"audio": 10`
   （约在 `COST = {"image": 12, "copy": 3, "audio": 4, "video": 0}` 这行）
2. **前端** `site/workbench/audio.html`：「生成配音」按钮旁的 `4 点` → `10 点`
   （约在 `生成配音 <span ...>4 点</span>`）

## 边界与注意
- **只改这两处数值**，不动其它成本、不改扣点/退点逻辑本身（点数走 auth 服务，见 T6，本工单不涉及）。
- 前后端数字必须一致（都是 10）。
- 仅配音（TTS，kind=`audio`）改动；作图/文案/视频等成本**不变**。

## ⚠️ 排期（重要）
本工单改 `content_api.py`，**与 T8（content_api 拆分）冲突**。**T8 若在进行中，本工单要与 T8 错开**：
- 要么在 T8 合并后、基于新结构改（届时 audio 成本可能已挪到某域模块，按实际位置改）；
- 要么先做本工单（很小），T8 重做时基于含本改动的 main。
**别和 T8 同时派给两个 agent 改 content_api。**

## 验收标准
1. 前端「生成配音」显示 `10 点`。
2. 实际生成一次配音扣 **10 点**（后端 COST 生效）。
3. 其它能力点数不变；无控制台报错、`py_compile` 通过、health 200。

## 部署与验证
- `./ship "配音点数 4→10 (Issue #60)" server/content_api.py site/workbench/audio.html`
- 上线后：`/workbench/audio` 显示 10 点；发一次配音确认扣 10；确认漂移哨兵无新增告警。
- 完成后在 **Issue #60 回复** qilin 已上线。

> 本工单只做规划，未改动任何文件。
