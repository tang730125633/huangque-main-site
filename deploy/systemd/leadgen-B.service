[Unit]
Description=Douyin Leadgen API slot B (blue-green)
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/leadgen-B
# 口令/令牌不再写死在单元里（旧值已作废）。真实值放服务器本地 /etc/leadgen-secrets.env（权限 600，不进 git）。
EnvironmentFile=/etc/leadgen-secrets.env
ExecStart=/home/ubuntu/MediaCrawler/.venv/bin/uvicorn app:app --host 127.0.0.1 --port 8092
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
