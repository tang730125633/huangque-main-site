# Python dependencies shared by Huangque content and admin services (/usr/bin/python3).
# Install with the same interpreter used by the systemd units:
#   /usr/bin/python3 -m pip install -r deploy/requirements-content.txt
# Admin customer-page QA also needs: /usr/bin/python3 -m playwright install chromium
cos-python-sdk-v5==1.9.44
# 主站路由与浏览器回归会导入 Flask。
Flask>=3.1,<4
# Seedance reference images are fully decoded before private staging.
Pillow==12.2.0
# CPU-only subtitle timing for talking-head and one-click composition.
# Model weights must be preloaded during deployment; requests run offline.
faster-whisper==1.2.1
playwright==1.45.0
# Provider credentials are encrypted at rest by the admin/content services.
cryptography>=45,<51
# Windows and minimal Python images do not ship the IANA timezone database.
tzdata>=2025.2
# PostgreSQL runtime + versioned schema migrations. SQLite remains authoritative
# until each business domain passes its explicit cutover gate.
psycopg[binary,pool]>=3.2,<4
SQLAlchemy>=2.0,<3
alembic>=1.13,<2
# M3F 缓存切 Redis TTL 后需要（HQ_TIKHUB_CACHE=redis 时启用）。
redis>=4.6,<6
